# 인수인계 노트

**작성**: 2026-08-19 (수) 09:32 KST · 맥(M4, `jeongsanghwa`) → 윈도우 업무용 노트북
**최종 갱신**: 2026-08-25 · 2022 1단원 슬라이드 9개 전수 검토 + `\times` 탭 버그 수정 (2-D절)

---

## 1. 브랜치 지도

윈도우에서 clone하면 **worktree 구조가 아니라 브랜치들**을 받습니다.
맥의 `.claude/worktrees/` 레이아웃은 gitignore라 넘어오지 않습니다. 재현하지 마시고 `git checkout`으로 오가시면 됩니다.

**2026-08-19 병합 완료.** `main`만 보시면 됩니다.

| 브랜치 | 내용 | 상태 |
|---|---|---|
| `main` | 아래 둘이 모두 들어 있음 | ✅ 여기서 작업 |
| `lesson/ai-math-2022-1-1` | 인공지능수학(2022) 1-1 1~2차시 수업자료 (`3b40f30`) | main에 병합됨 |
| `claude/reverent-moser-21ec5b` | Pretendard 폰트 로컬 번들링 + 이 노트 | main에 병합됨 |
| `claude/strange-villani-e75dbb` | **내용 없음** — main과 동일한 빈 브랜치 | 원격에 없음. 무시 |

두 브랜치는 건드리는 파일이 겹치지 않아 충돌 없이 병합됐습니다.
병합된 브랜치들은 삭제하셔도 됩니다.

## 2. 직전 작업 — Pretendard 폰트 번들링 (완료·검증됨)

`_shared/templates/design-tokens.css`가 폰트 공급원 두 개를 선언하는데 **둘 다 404**였습니다.

- CDN `@import`의 `pretendard@2.3.0` — 존재하지 않는 태그 (최신은 1.3.9)
- 로컬 `@font-face`의 `../fonts/PretendardVariable.woff2` — `_shared/fonts/` 자체가 없었음

결과적으로 Pretendard는 그동안 **전혀 적용되지 않고** 맑은 고딕으로 렌더링되고 있었습니다.

**조치**: `_shared/fonts/PretendardVariable.woff2`(1.96MB, SIL OFL 1.1, LICENSE 동봉)를 저장소에 번들링하고 깨진 CDN `@import`를 제거. 이제 외부 의존 0.

**검증 완료** (재확인 필요 없음):
- 서버 로그 404 = `/favicon.ico` 2건뿐, 폰트·CSS는 전부 200/304
- `document.fonts`에 `Pretendard Variable|45 920|loaded`
- 폭 실측: Pretendard 1130.57px vs 폴백 1070.91px → 실제 적용됨
- `design-tokens.css` 내 외부 URL **0개**

재검증이 필요하면 (포트 8765는 다른 프로세스가 점유 중이니 피할 것):

```
python3 -m http.server 8791
```

띄운 뒤 `_shared/templates/reference/worksheet-kitchen-sink.html` 을 엽니다.
⚠️ `output/` 산출물 HTML로는 검증이 안 됩니다 — 아래 3번 참조.

## 2-B. 유사도 덱 재작성 (2026-08-24, 맥에서 완료·검증됨)

`ai-math/output/slides/슬라이드_III-1-03_유사도분석.html`을 **같은 경로에 전면 교체**했습니다.
구판(다크 테마·362줄)은 `git show HEAD^:"경로"`로 복원 가능합니다.

**왜 교체했나**
- 다크 테마 구판 — `--lm-*` 토큰 미적용, 외부 Google Fonts 참조
- 발표자 노트 0개
- **군집화 결론이 교과서와 달랐음** — 3그룹으로 마무리했으나 교과서 p.72는 **2그룹**
  (`C(G₂,T₃)=0.87`이 최대라 T₃이 G₂에 붙어 `G₃={T₃,T₄,T₅}`). 이번에 바로잡았습니다.

**무엇이 새로 들어갔나**
세 유사도의 **장단점 비교**(2015 교과서 본문에 없는 내용). 근거는 2022 개정 교과서 Ⅱ-3(pp.72~73)의
환경 문서 A/B/C 예제 — **세 척도가 서로 다른 답을 내도록 설계된 사례**입니다.
화면에는 "교과서 밖 보조 자료"로만 표시하고 교육과정 이야기는 하지 않습니다.
표기는 2015판(`L(P,Q)`, `(pᵢ-qᵢ)²`)으로 통일했습니다.

설계 근거·수치·수업 운영 지시는 `docs/plans/2026-08-24-유사도-세척도-비교덱.md` 참조.

**검증 (맥, 포트 8791)** — 모든 인터랙션을 **실제 클릭으로** 최대 전개해 측정.
세로 최대 **942 / 상한 994**, 가로 넘침 0, 콘솔 에러·404 0, MathJax 오류 0·원시 TeX 잔존 0,
노트 16/16, 도식 4개 배율 1.00·잘림 0·라벨 겹침 0,
교과서 값 `pdftotext` 원문 대조 일치, 반례 수치 Python 검산 일치.

### ✅ 정정 (2026-08-25) — step-box 는 고장 나 있지 않았다

2026-08-24에 "공유 CSS 의 step-box 전이가 Reveal 의 display 토글과 겹쳐 펼쳐지지 않는다"고
진단해 커밋(`0dad0db`)했는데 **이 진단은 틀렸습니다.**

진짜 원인은 검증에 쓰던 **브라우저 창이 `document.visibilityState === "hidden"`** 이었던 것입니다.
크롬은 숨겨진 문서에서 CSS 전이를 시작하지 않아, 클래스는 붙었는데 계산 스타일이
시작 값에 머물렀습니다. 같은 문서에서 시험한 결과:

```
전이 없음  opacity 0.1 → 1      바뀜
전이 있음  opacity 0.1 → 0.1    안 바뀜      (visibilityState: hidden)
```

**형제 덱들(1-1-3, 1-2-2, 1-2-3, 1-2-4, 1-3-1, 1-3-2)은 멀쩡합니다.** 손볼 것 없습니다.

그래도 유효한 것 둘:

1. **명시도 함정은 진짜입니다.** `section.tight .step-box`(0,2,1)는 공유 CSS 의
   `.reveal .step-box.visible`(0,3,0)에 집니다. **여백은 `.visible` 쪽에 둬야 이깁니다.**
   이걸 몰라 8장이 1023px 로 넘쳤습니다.
2. **전이·애니메이션이 걸린 것을 재기 전에 `document.visibilityState` 를 먼저 확인하세요.**
   `hidden` 이면 전이 기반 상태 측정은 전부 무효입니다. 높이만 재는 건 영향 없습니다.

유사도 덱의 step-box 는 오진 과정에서 `display` 토글로 바꿨고 그대로 두었습니다 —
전이가 없어 확실히 동작하고 이미 검증을 마쳤기 때문입니다. 표시 조건은 규약대로 `.visible` 하나입니다.

**로컬 사람 눈 검토에서 남은 확인 사항** (수업 전 실제로 열어 볼 것)
- 11장에서 **유클리드를 마지막에** 눌러야 반례의 충격이 삽니다. 노트에 적어 두었습니다.
- 14장(군집화)은 컷 라인입니다. 시간이 밀리면 건너뛰고 15장으로 갑니다.
- 13장 "주의할 점" 행이 "유클리드가 틀렸다"로 읽히지 않는지 — 프레이밍이 12장과 이어져야 합니다.

**도식 규칙 (새로 정한 것)** — 모든 `svg.dg`는 **viewBox 단위 = 렌더 px(배율 1.00)**로 그립니다.
그래야 `.dg .t-lab: 34px`이 어느 장에서든 무대 위 34px이 됩니다(본문 36px). 초안에서 배율이
1.00/1.78/1.43로 제각각이라 같은 클래스가 장마다 다른 크기로 렌더됐습니다.

---

## 2-C. Ⅲ-1 이미지 분류 3차시 + 학습지 (2026-08-25, 맥에서 완료·검증됨)

교과서 73~82쪽을 3차시로 만들고 학습지 3종을 붙였습니다.

| 차시 | 슬라이드 | 학습지 | 교과서 |
|---|---|---|---|
| ④ | `슬라이드_III-1-04_퍼셉트론과가중치.html` (12장) | 있음 | 73~75쪽 |
| ⑤ | `슬라이드_III-1-05_퍼셉트론과논리연산.html` (12장) | 있음 | 75~77쪽 |
| ⑥ | `슬라이드_III-1-06_이미지분류_해밍과퍼셉트론.html` (16장) | 있음 | 78~82쪽 |

구판 `슬라이드_III-1-02_이미지분류.html`(10장, 73~82쪽을 한 덱에 몰아넣음)은 삭제했습니다.
**`대본_III-1-02_이미지분류.html`과 `학습지_III-1-02_이미지분류.html`은 그대로 두었습니다** —
짝이 없어진 고아 파일이니 정리 여부는 판단이 필요합니다.

### 교과서 82쪽 검토 (사용자 질문에 대한 답)

퍼셉트론 분류 절을 없앨 필요는 없지만 **구성에 구멍이 있습니다.** 요약:

- 퍼셉트론이 계산하는 값은 \(|C \cap A| / |A|\) — "정답의 켜진 칸 중 몇 %가 입력에도 켜졌나"
- **잘못 켜진 칸을 벌하지 않습니다.** 전광판을 전부 켜면 0일 가능성도 8일 가능성도 1.00
- **교과서 문제 3이 그 구멍을 밟습니다** — \(P'=17/17=1.00\)인데 해밍으로는 4칸이 다름
- 이 예제에서는 **분자가 둘 다 14**(C가 A에도 B에도 통째로 들어 있어서)라
  답을 가른 것은 분모뿐 — 모양이 아니라 "어느 쪽이 덜 켜졌나"만 본 셈

그래서 6차시 후반을 유사도 덱의 유클리드 반례와 **같은 구조**로 짰습니다.
프레이밍은 **"퍼셉트론이 틀렸다"가 아니라 "재는 것이 다르다"** 입니다.
자세한 근거는 `docs/plans/2026-08-25-이미지분류-3차시와-82쪽-검토.md`.

### 표기 주의 (2015판)

임곗값 **θ**, 활성화함수 **σ(x)**, 출력 **0 또는 c**.
2022 덱들이 쓰는 `h`와 다릅니다. **섞지 마세요.**

### `data-subject` 정정

`ai-math`(하이픈)이 아니라 **`ai_math`(밑줄)**이 맞습니다.
`design-tokens.css`가 정의한 값이 밑줄 쪽이라 하이픈은 아무 규칙에도 안 걸립니다.
(`:root` 기본값이 같은 오크색이라 화면 차이는 없었지만 속성이 죽어 있었습니다.)
Ⅲ-1-03~06 네 개를 고쳤습니다. **새 덱은 `ai_math`로 쓰세요.**

### 검증 (맥, 포트 8791 · 실제 클릭 기준)

세로 최대 ④ 901 / ⑤ 960 / ⑥ 944 (상한 994), 넘침 0, 가로 넘침 0,
도식 배율 전부 1.00·잘림 0·라벨 겹침 0, MathJax 오류 0, 원시 TeX 0, 노트 전 장 1개씩.
픽셀 격자 실측 대조 — A 19 / B 17 / C 14 / D 21칸, H(C,A)=5, H(C,B)=3, 겹침 14/14.
학습지 3종 모두 A4 2쪽 (여유 04: 11.4·23.6mm, 05: 10.6·7.2mm, 06: 4.2·4.7mm).

### 학습지는 3쪽 구조입니다 (2026-08-25 변경)

**1~2쪽은 학생, 3쪽은 교사용 정답**입니다(3쪽 제목에 "교사용" 배지).
종전 2022 학습지처럼 2쪽에 심화와 정답을 같이 두면 답이 같이 넘어가서 분리했습니다.
**새 학습지도 이 구조로 만드세요.**

### 82쪽 프레이밍 (2026-08-25 재조정)

초안은 반례를 클라이맥스에 놓아 퍼셉트론을 심문하는 모양새였는데, 그건 원래 문제 제기가
아니었습니다. 정직한 답은 이렇습니다 — **이 문제 안에서는 해밍 거리만으로 충분하고,
퍼셉트론이 새로 하는 일이 없습니다.** 82쪽이 있는 이유는 이 문제를 더 잘 풀려고가 아니라
**퍼셉트론 단원과 실제 신경망 분류기 사이에 다리를 놓으려고**입니다.

덱 06 후반부를 그 취지로 다시 썼습니다 —
11장 "왜 한 번 더 푸는가?" → 12장 "이 문제엔 해밍이면 됐다" → 13장 "다음을 위해서다"
(해밍=견본 대 보기 / 퍼셉트론=점수 매겨 고르기, 손글씨처럼 견본을 못 정할 때 학습이 필요).
반례는 14장 '한계' 행과 확인 ⑵에만 근거로 남겼습니다.

---

## 2-D. 인공지능수학(2022) 1단원 슬라이드 9개 전수 검토 (2026-08-25, 맥)

2학년(2022 개정) 슬라이드에 "수식이 깨지고, 적용 안 된 것도 있다"는 신고로 전수 검토했습니다.

### 실제로 고친 것 — `\times`가 탭 문자로 저장돼 있었음

`슬라이드_1-1-3차시_퍼셉트론이작동하는법.html` 10쪽 STEP 1 (커밋 `5c32471`).

```
저장돼 있던 것:  \( x = 1 [TAB]imes 0.8 + 2 [TAB]imes 0.3 \)
화면 출력:       x = 1imes0.8 + 2imes0.3
고친 것:         \( x = 1 \times 0.8 + 2 \times 0.3 \)
```

생성할 때 문자열 이스케이프 `\t`가 탭으로 해석된 것입니다. **아래 「함정」 절에 검출법을 적어 뒀습니다.**

### 나머지 8개는 이상 없음 (기계 검사 기준)

포트 8791, 숨은 장까지 `textContent`로, 인터랙션 펼친 상태.

| 차시 | 장 | 수식 | MathJax 오류 | 원시 TeX |
|---|---|---|---|---|
| 1-1-1 데이터로배우는기계 | 11 | 0 | 0 | 없음 |
| 1-1-2 정답을줄까보상을줄까 | 12 | 0 | 0 | 없음 |
| 1-1-3 퍼셉트론이작동하는법 | 14 | 52 | 0 | 없음(수정 후) |
| 1-2-1 인공지능70년과순서도 | 14 | 12 | 0 | 없음 |
| 1-2-2 진리표와논리연산 | 14 | 88 | 0 | 없음 |
| 1-2-3 퍼셉트론과논리연산 | 14 | 124 | 0 | 없음 |
| 1-2-4 다층퍼셉트론과논리회로 | 14 | 106 | 0 | 없음 |
| 1-3-1 빅데이터의의미 | 10 | 9 | 0 | 없음 |
| 1-3-2 빅데이터활용과공정성 | 13 | 6 | 0 | 없음 |

**1-3절 신판이 이미 있습니다.** 3절의 "1-3절은 신판이 아직 없습니다"는 낡은 기록입니다.

### 측정 실패 두 건 — 같은 실수 반복하지 말 것

1. **`innerText`로 원시 TeX를 찾으면 안 됩니다.** Reveal는 현재 장 외를 숨기고,
   숨은 요소의 `innerText`는 빈 문자열입니다. 나머지 13장을 통째로 못 봅니다.
   → **`textContent`를 쓰세요** (`script`·`style`·`mjx-container` 제거 후).
2. **모든 section에 `display:block; height:auto`를 강제해 놓고 재면 안 됩니다.**
   레이아웃 맥락이 바뀌어 값이 부풀려집니다. 이 방식으로 3장이 994px 초과로 나왔지만,
   `Reveal.slide(i)`로 하나씩 옮겨 가며 재니 **최대 912px로 전부 이내**였습니다.

### step-box는 (또) 고장 나 있지 않았습니다

74행의 2026-08-25 정정과 같은 함정에 다시 걸렸습니다.
`querySelectorAll('button,[onclick],.btn')`을 전부 `.click()`하면 **"처음으로"까지 눌러
초기화**되고, `.visible`을 직접 붙여도 슬라이드 자체 JS와 충돌해 `offsetHeight: 0`이 됩니다.
→ **"다음 단계" 버튼만 실제 좌표로 클릭해서 확인하세요.** 그렇게 하니 3단계 모두 정상이었습니다.

### 신고의 나머지 절반 — 3월판 학습지 3개

`ai-math-2022/output/worksheet/`에 2026-03 제작본 3개가 남아 있습니다.
**MathJax를 아예 쓰지 않고** 수식을 유니코드 문자(`x₁`, `σ`, `≥`, `⊻`)로 흉내 냈습니다.
"수식이 적용 안 된 것"은 이쪽입니다. 3절에 잔여 작업으로 옮겼습니다.

---


## 2-E. Ⅲ-2 경향성과 예측 (2026-08-26, **덱 4차시 완료** · 학습지 보류)

네 차시 덱을 모두 만들고 기계 검증까지 마쳤습니다. **학습지 4종은 사용자 요청으로 뒤로 미뤘습니다**(2026-08-26).
매시 :19 스케줄(`lesson-maker-hourly-review`)은 이제 **2022 2단원**을 대상으로 바뀌었습니다.

**진행 상황**: 교과서 84~95쪽 **4차시 덱 전부 완료**(2026-08-26). 학습지 4종이 남았습니다. 설계 근거와 차시별 메모는
`docs/plans/2026-08-26-III-2-경향성과예측-차시분할.md` (커밋 `b861db1`).

| 차시 | 파일 | 교과서 | 상태 |
|---|---|---|---|
| ① | `슬라이드_III-2-01_확률로예측하기.html` | 84~86 | **덱 완료** (12장, 세로 max 982/994) · 학습지 미착수 |
| ② | `슬라이드_III-2-02_조건이겹칠때.html` | 87~89 | **덱 완료** (13장, 세로 max 932/994) · 학습지 미착수 |
| ③ | `슬라이드_III-2-03_산점도와추세선.html` | 90~92 | **덱 완료** (10장, 세로 max 919/994) · 학습지 미착수 |
| ④ | `슬라이드_III-2-04_예측값과오차.html` | 93~95 | **덱 완료** (10장, 세로 max 965/994) · 학습지 미착수 |

학습지는 덱이 끝난 차시부터 3쪽 구조로 붙입니다.

### 이어받는 세션이 알아야 할 것
- **구판 4개는 삭제됐습니다** (2026-08-26, 사용자 승인). 절 단위였던 `슬라이드_III-2-01`·`02`와
  같은 이름의 학습지 2개입니다. 그래서 새 덱이 번호 **01~04**를 씁니다.
  복구가 필요하면 `git show <삭제커밋>^:<경로>`.
- **종합문제 배지는 아직 옛 번호입니다** — `ai-math/output/exam/종합문제_대단원III_분류와예측.html`이
  문항마다 `III-2-01`·`III-2-02` 배지를 답니다. 링크가 아니라 텍스트라 깨지진 않지만,
  네 차시가 다 나온 뒤 문항별로 다시 매겨야 합니다.
- **지도서가 금한 것 두 가지** — ①"조건부확률" 용어·기호 도입 금지(상대도수 수준으로만)
  ②수치 계산 치중 금지. 추세선도 최소제곱 유도 없이 공학적 도구로 구합니다.
- **Ⅲ-2에는 퍼셉트론이 안 나옵니다.** θ·σ 표기 주의는 대단원 마무리에만 해당합니다.
- 통그라미(91~92쪽)는 웹 도구 화면이라 캡처가 없습니다. 순서 카드로 대체했습니다.

### 사람 눈 검토 대기

기계 검사(세로 994·콘솔·MathJax·노트·교과서 값 대조)는 자율 작업에서 수행하지만,
**도식이 뒷자리에서 보이는가·설명 흐름이 자연스러운가는 검토되지 않았습니다.**
수업 전 로컬에서 반드시 확인하세요.
## 2-F. 인공지능수학(2022) Ⅱ단원 텍스트 데이터 처리 (2026-08-26, **진행 중**)

⚠️ **1단원은 다른 세션이 수정 중입니다**(2026-08-26 사용자 확인).
이 작업은 **2단원 파일만** 새로 만듭니다. `ai-math-2022/output/*/…1-*` 파일은 건드리지 마세요.

교과서 48~82쪽을 **8차시**로 분할했습니다. 설계 근거와 차시별 메모는
`docs/plans/2026-08-26-2022-2단원-텍스트데이터-차시분할.md`.

| 차시 | 파일 | 교과서 | 상태 |
|---|---|---|---|
| 2-1-1 | `슬라이드_2-1-1차시_정형과비정형그리고벡터.html` | 50~52 | **덱 완료** (11장, 세로 max 830/994) |
| 2-1-2 | `슬라이드_2-1-2차시_문장을집합으로.html` | 52~54 | **덱 완료** (13장, 세로 max 893/994) |
| 2-2-1 | `슬라이드_2-2-1차시_빈도수벡터와주제어.html` | 55~58 | **덱 완료** (11장, 세로 max 772/994) |
| 2-2-2 | `슬라이드_2-2-2차시_TF-IDF.html` | 58~62 | 미착수 |
| 2-3-1 | `슬라이드_2-3-1차시_감성사전과감성점수.html` | 63~66 | 미착수 |
| 2-3-2 | `슬라이드_2-3-2차시_벡터의덧셈과전체감성.html` | 66~68 | 미착수 |
| 2-3-3 | `슬라이드_2-3-3차시_유클리드와코사인유사도.html` | 68~70 | 미착수 |
| 2-3-4 | `슬라이드_2-3-4차시_자카드유사도와비교.html` | 71~75 | 미착수 |

### 이어받는 세션이 알아야 할 것

- **2022판 표기입니다.** 임곗값은 **h**(2015의 θ가 아님). 3학년 2015 덱과 섞지 마세요.
- **TF-IDF는 2022에만 있습니다.** 2015 트랙에는 없으니 참고할 덱이 없습니다.
- 61쪽 \(IDF=\log\frac{n}{DF}\)는 **『대수』 이수자 대상 심화**입니다. 선택 자료로 표시하세요.
- 복사 출발점은 `ai-math-2022/output/slides/슬라이드_1-3-2차시_빅데이터활용과공정성.html`.
- **SVG `<text>` 안에 수식 금지** — Ⅲ-2-03에서 실제로 걸렸습니다(조판은 되나 렌더 안 됨).


### ⚠️ 2026-08-26 사고 기록 — 다른 세션 파일이 섞여 커밋됨

2-1-1차시 커밋(`30270cf`)에 **1단원 파일이 함께 실려 push됐습니다.**
`ai-math-2022/output/slides/슬라이드_1-2-4차시_다층퍼셉트론과논리회로.html` 272줄
(5쪽 SVG 단계 노출, 8쪽 계산 행 표시 등)이 그 세션의 작업물인데,
제가 `git add -A`를 써서 함께 담겼습니다.

- **유실은 없습니다.** 파일은 구조적으로 온전하고(섹션 14 열림/닫힘 일치, 노트 14) 내용도 그대로입니다.
- 다만 **그 세션의 검증을 거치지 않은 상태**로 main에 올라갔고, 커밋 메시지에도 그 내용이 없습니다.
- 1단원 작업 세션은 이어서 자기 커밋을 하면 됩니다. 되돌리지 마세요 —
  되돌리면 오히려 그 세션의 작업이 사라집니다.

**재발 방지**: 이 저장소에서 여러 세션이 동시에 돌 때는 `git add -A`·`git add .` 금지.
**자기가 만들거나 고친 파일만 경로로 지정해 add**할 것. 매시 스케줄 작업 프롬프트에도 넣었습니다.
### 3학년(2015) Ⅲ-2와의 관계

2022 **4단원**(4-1 확률예측 · 4-2 추세선)이 오늘 만든 2015 Ⅲ-2와 같은 주제입니다.
4단원을 만들 때 `슬라이드_III-2-0{1,2,3,4}_*.html`을 상당 부분 재활용할 수 있으나,
교과서가 다르므로 숫자·예제는 2022판으로 다시 확인해야 합니다.

## 3. 다음에 할 일

### 0. 2022 3월판 학습지 3개 재제작 (2026-08-25 합의, 착수 전)

`ai-math-2022/output/worksheet/` — 사용자가 "새 템플릿으로 다시 만들기"로 결정했습니다.

```
학습지_1-1_인공지능의의미.html
학습지_1-2_인공지능의역사.html
학습지_1-3_빅데이터와인공지능.html
```

고칠 점:
- **MathJax가 없습니다.** 수식이 유니코드 문자(`x₁`, `w₁`, `σ`, `≥`, `∧`, `∨`, `⊻`)라 조판이 안 됩니다
- 구판 강조색 `#f39c12` 하드코딩 → `data-subject="ai_math"`로 `--lm-accent`(`#8F6A1F`) 사용
- **XOR 표기가 교과서와 다릅니다** — `⊻`(∨ 아래 막대)를 썼는데 교과서는 **∨ 위에 점**입니다.
  슬라이드가 쓰는 매크로 `macros: { xor: '\\mathbin{\\dot{\\vee}}' }`를 그대로 쓰세요.
  ⚠️ **유니코드 `∨̇`(U+2228 + U+0307)를 직접 쓰면 두부 박스로 깨집니다.** 렌더링 비교로 확인했습니다
- 절 단위(1-1/1-2/1-3)라 **현행 차시 단위 구성과 안 맞습니다** — 1-1은 3차시로 나뉘어 있습니다
- 2-C의 **3쪽 구조**(1~2쪽 학생 / 3쪽 교사용 정답)를 따를 것

### 그 밖 (기존 3건)

1. **산출물 90개가 디자인 시스템과 단절** — `output/` 하위 HTML 중 `design-tokens.css`를 참조하는 파일이 **0개**입니다. 전부 Google Fonts(Noto Sans KR)를 직접 링크합니다. 즉 이번 폰트 수정의 혜택이 실제 수업자료에는 닿지 않습니다. 가장 큰 건.
2. **템플릿 상대경로 깊이 불일치** — `reveal_base.html:48`·`worksheet_base.html:11`이 `../../_shared/`를 쓰는데 산출물 89/90개는 `<과목>/output/<하위폴더>/` 3단계라 `../../../_shared/`가 필요합니다. 템플릿을 그대로 복사하면 CSS가 404 납니다. 1번을 하려면 이것부터.
3. `--lm-font-mono: 'JetBrains Mono'`에 대응하는 `@font-face`가 저장소 어디에도 없습니다.

### 2026-04판 구판 슬라이드·대본 6건 삭제됨 (2026-08-20)

`ai-math-2022/output/slides/`에서 절 단위 구판 6개를 지웠습니다(커밋 `8dc574c`).
차시 단위 신판 7개가 1-1·1-2절을 완전히 대체했고, 구판은 **애초에 실행되지도 않았습니다** —
`../../libs/`의 자산 8개(reveal·mathjax·gsap·p5·chart 등)를 참조하는데 그 경로는
저장소에 존재한 적이 없어 `typeof Reveal === 'undefined'` 상태였습니다.
테마도 `theme/black.css`(다크)라 현행 Pretendard 밝은 디자인 시스템과 다릅니다.

~~**단, 1-3절(빅데이터)은 신판이 아직 없습니다.**~~
**정정 (2026-08-25): 1-3 신판 2개가 이미 있습니다** —
`슬라이드_1-3-1차시_빅데이터의의미.html`(10장), `슬라이드_1-3-2차시_빅데이터활용과공정성.html`(13장).
둘 다 2-D 검토에서 이상 없음. 아래 구판 추출 명령은 참고용으로만 남깁니다.

```
git show 8dc574c^:"ai-math-2022/output/slides/슬라이드_1-3_빅데이터와인공지능.html" > /tmp/ref-1-3.html
git show 8dc574c^:"ai-math-2022/output/slides/대본_1-3_빅데이터와인공지능.html"   > /tmp/ref-daebon-1-3.html
```

`ai-math/output/slides/`의 IV-1·IV-2도 같은 libs 문제를 안고 있으나 2015 교육과정이라 방치 중입니다.

### 슬라이드 세로 상한은 1080px입니다 (측정 근거)

Reveal 스테이지가 1920×1080이고 `.slides`의 clientHeight가 1080, section은
`position:absolute`라 내용만큼 늘어납니다. 브라우저 실측 결과 **1025px짜리 장에서
`scrollHeight === clientHeight`이고 잘림이 없었습니다**(2026-08-20 맥).
즉 1080이 실제 한계이고 그보다 낮은 수치는 안전 여백입니다.
여백을 얼마나 둘지는 취향이지만, **상한을 몇으로 잡았는지 커밋 메시지에 적어 두세요** —
두 기기에서 서로 다른 상한으로 같은 파일을 조정하면 도식 크기가 왔다 갔다 합니다.

### ⚠️ 슬라이드 작성 시 함정 — 수식 안의 `<`

MathJax 수식 안에서 **`<` 바로 뒤에 알파벳이 오면 브라우저가 HTML 태그 시작으로 파싱해
그 줄의 나머지를 통째로 삼킵니다.** 콘솔 에러도, MathJax 에러도 나지 않아 눈으로만 발견됩니다.

```
\(x<h\)        → <h ... 가 태그로 먹힘 (깨짐)
\(0<h\le 2\)  → 깨짐
\(x &lt; h\)   → 정상
\(x < 1\)      → 정상 (뒤가 숫자·공백이면 태그가 아님)
```

**규칙: 수식 안의 `<`는 항상 `&lt;`로 쓴다.** 2026-08-20 1-2-3차시 덱에서 실제로 겪었습니다.
검출: `grep -nE '<[a-zA-Z]' 파일 | grep -v '</\?\(div\|span\|b\|td\|tr\)'`

### ⚠️ 함정 2 — LaTeX 명령이 문자열 이스케이프로 먹힘 (`\times` → 탭)

`\t` `\b` `\f` `\v` `\a` `\0`으로 시작하는 LaTeX 명령을 **JS/Python 문자열 안에서** 만들면
이스케이프로 해석돼 제어문자 + 나머지 글자만 남습니다. 콘솔 에러가 안 납니다.

```
\times → [TAB]imes      \begin → [\b]egin     \frac → [\f]rac
\text  → [TAB]ext       \beta  → [\b]eta      \vee  → [\v]ee
\theta → [TAB]heta      \bullet→ [\b]ullet    \vec  → [\v]ec
```

2026-08-25 1-1-3차시 10쪽에서 실제로 겪었습니다(`5c32471`).
**버튼을 눌러야 나오는 `.step-box` 안이라 로드 시점 검사로는 안 잡혔습니다.**

검출 — 저장소 전체를 한 번에 (2026-08-25 실행 결과 0건):

```bash
python3 - <<'PY'
import glob, io, re
CTRL={'\t':'\\t','\x08':'\\b','\x0c':'\\f','\x0b':'\\v','\x07':'\\a','\x00':'\\0'}
TAILS=['imes','ext{','ext ','heta','ilde','op ','o ','an ','eq','abla','u ','ot ',
       'ho','ight','egin','eta','ullet','ar{','mod','rac','orall','ee','ec{',
       'arphi','ert','arepsilon','lpha','pprox']
for f in sorted(glob.glob('**/*.html',recursive=True)):
    s=io.open(f,encoding='utf-8').read()
    for ch,name in CTRL.items():
        for m in re.finditer(re.escape(ch),s):
            if any(s[m.end():m.end()+12].startswith(t) for t in TAILS):
                print(f'{f} L{s[:m.start()].count(chr(10))+1} {name}')
PY
```

**예방: 생성 코드에서 TeX는 raw 문자열(`r'\times'`)이나 이스케이프(`'\\times'`)로 쓰세요.**

## 4. 기기 전환 시 주의

- **저장소 안에 없는 것은 넘어오지 않습니다.** 계획 파일은 맥의 `~/.claude/plans/`에 있어 윈도우에 없습니다. 대화 내용도 마찬가지입니다. 남길 게 있으면 이 파일에 적으세요.
- **계획서는 `docs/plans/`에 남깁니다.** `~/.claude/plans/`는 기기를 넘지 못하므로, 계획이 확정되면 `docs/plans/YYYY-MM-DD-<주제>.md`로 저장소에 복사해 커밋합니다. 결과물(코드)만 넘어오고 판단 근거가 사라지는 걸 막기 위해서입니다. 실례: `docs/plans/2026-08-19-pretendard-bundling.md` — 커밋 `bcb12e4`의 근거 문서로, 폰트 조사 실측치(폴백 1070.91px vs Pretendard 1130.57px 등)가 들어 있습니다.
- **새 기기 설정의 정본은 `README.md`의 "다른 기기에서 작업하기"입니다.** 전체 clone + `pip install -r requirements.txt` + `playwright install chromium`. 교과서 PDF는 수업 설계에 쓰이므로 저장소에 **일부러 포함**돼 있고, 지도서(52MB)만 제외돼 Google Drive에서 따로 복사합니다.
- **⚠️ 지도서는 git에 없습니다 — 새 차시를 만들려면 반드시 먼저 복사하세요.**
  `*/book/지도서/`가 `.gitignore`에 있습니다(용량 52MB). 지도서에는 **성취기준 해설·지도 목표·
  차시 배분표·문항 풀이**가 들어 있어, 이것 없이 차시를 설계하면 배분을 틀립니다.
  (실례: 2026-08-25에 1-1을 2차시로 잡았다가 지도서 배분표를 보고 **4차시**로 정정했습니다.
  Ⅰ단원은 총 14차시 — 대단원 도입+1-1이 4, 1-2가 5, 1-3이 3, 마무리가 2입니다.)

  맥:
  ```bash
  mkdir -p ai-math-2022/book/지도서
  cp ~/"내 드라이브(juvilan0429@gmail.com)/001_영일고/1_학교업무/01_수업/교과서/2022_인공지능수학/미래엔_인공지능수학 지도서(2022개정)"/*.pdf \
     ai-math-2022/book/지도서/
  ```
  윈도우(PowerShell) — **드라이브 문자는 각자 환경에 맞게 확인하세요.** Google Drive 데스크톱은
  보통 `G:` 로 잡히며, `Get-PSDrive` 로 확인할 수 있습니다.
  ```powershell
  New-Item -ItemType Directory -Force ai-math-2022\book\지도서
  Copy-Item "G:\내 드라이브\001_영일고\1_학교업무\01_수업\교과서\2022_인공지능수학\미래엔_인공지능수학 지도서(2022개정)\*.pdf" `
            ai-math-2022\book\지도서\
  ```
  복사 후 15개가 맞는지 확인하세요. **맥에서 원본과 md5 대조 결과 교과서 PDF는 저장소 사본과 동일**하므로, 교과서는 다시 복사할 필요가 없습니다.
- (선택) 학교 노트북 용량이 부담되면 교과서 PDF를 빼고 받을 수도 있습니다. clone 약 283MB 중 대부분이 `*/book/*.pdf`(316MB)입니다. 다만 PDF 없이는 교안 설계 시 교과서 참조가 안 되니, **기본은 README대로 전체 clone**을 권합니다.
  ```
  git clone --filter=blob:none --no-checkout https://github.com/juvilan/lesson-maker.git
  cd lesson-maker && git sparse-checkout set --no-cone '/*' '!/*/book/' && git checkout main
  ```
  ⚠️ **이 명령은 쓰지 마세요 — 검증 결과 실패입니다.** 2026-08-19 윈도우에서 실제로 실행한 결과 `You are in a sparse checkout with 0% of tracked files present.` 로, 파일이 하나도 체크아웃되지 않은 빈 저장소가 만들어졌습니다. `--no-cone`의 포함 패턴 `'/*'`와 배제 패턴 `'!/*/book/'`이 충돌한 것으로 보입니다. 용량을 줄여야 한다면 cone 모드로 다시 작성해 **실제 clone으로 검증한 뒤** 이 자리에 적어주세요. (잘못 실행했다면 되돌리기: `git sparse-checkout disable` 또는 저장소를 지우고 README대로 전체 clone.)
- **습관**: 작업 시작 `/sync`, 작업 끝 `/commit-push-pr`. 이번에 `3b40f30`이 push 없이 맥에만 20일 남아 있던 게 이 습관이 빠져서였습니다.

## 5. 윈도우 노트북 환경 — 남은 작업 3건 (2026-08-19)

저장소가 아니라 **기기 설정** 쪽 잔여 작업입니다. 저장소 작업(3절)과 별개입니다.

1. **빈 껍데기 폴더 삭제** — `C:\Users\user\lesson-maker`. 4절의 sparse-checkout 명령이 만든 0% 체크아웃 저장소 자리로, 내용물(중복 clone 604MB + 깨진 `.git`)은 이미 지웠고 **빈 디렉터리만 남았습니다.** 그 폴더를 작업 디렉터리로 잡은 Claude Code 세션이 있으면 삭제가 막히니, `Projects\lesson-maker`에서 세션을 연 뒤 실행하세요.
   ```
   Remove-Item -Force C:\Users\user\lesson-maker
   ```
   ※ 이 저장소의 정본 위치는 `C:\Users\user\Projects\lesson-maker`입니다 (맥의 `~/Projects/lesson-maker`와 같은 배치).

2. **Python Store 별칭 정리** (GUI라 직접 해야 함) — 설정 → 앱 → 고급 앱 설정 → 앱 실행 별칭에서 **`python3` (3.13) 항목 끄기**.
   **Python은 설치돼 있습니다 — 3.14.7** (`AppData\Local\Python\pythoncore-3.14-64`, pip 26.2.1). PowerShell에서는 `python`·`python3`·`py` 모두 정상입니다.
   그런데 **Git Bash에서만 `python3`이 항상 실패합니다**(`0x80070002`). 사라진 3.13 Store 패키지를 가리키는 확장자 없는 별칭 stub을 MSYS가 실행하지 못하는 것으로, **Bash에서 안 된다고 "Python 미설치"로 오진하기 쉽습니다**(실제로 2026-08-19에 그렇게 오진했습니다). 판단 전에 PowerShell로 교차 확인하세요.
   별칭을 정리하면 Git Bash에서도 `python3`이 살아나고, `~/.claude/skills/continuous-learning-v2/hooks/observe.sh`의 간헐적 blocking 에러도 사라집니다. (그 훅은 현재 `~/.claude/homunculus/disabled` 파일로 꺼둔 상태 — 파일을 지우면 다시 켜집니다.)
   PDF 파이프라인 의존성은 **설치·검증 완료**입니다: playwright 1.62.0 + pypdf 6.16.1 + Chrome Headless Shell 151. `html_to_pdf.py`로 kitchen-sink 8페이지 변환 확인.

3. **`~/.claude/rules/agents-v2.md`의 `~/qjc-office/` 경로** — 이 기기에 그 경로가 없어 참조 6개가 전부 끊깁니다. 같은 내용이 `C:\Users\user\Projects\claude-forge\`에 있고, `reference/`에 3개(`agents-config-ref.md`, `agents-teams-ref.md`, `server-inventory.md`)가 존재합니다.
   나머지 3개(`agent-catalog.md`, `agent-pipeline.md`, `parallel-agents-guide.md`)는 **claude-forge 저장소 어디에도 없습니다**(0/0 동기화 상태 확인). 맥에서 `ls ~/qjc-office/dotclaude/reference/`로 실재 여부를 먼저 확인하고, 있으면 claude-forge에 커밋·push, 없으면 rules에서 참조를 제거하세요.
   경로 통일 방식은 미결정입니다 — (A) 맥에 `~/Projects/claude-forge` symlink 후 rules를 그 경로로 통일 / (B) rules를 `claude-forge/reference/` 상대 표기로 / (C) 윈도우 경로로 바꾸고 맥은 나중에.
   ⚠️ **`~/.claude/rules/*.md`와 `~/.claude/settings.json`은 `claude-forge/`와 바이트 동일한 install 산출물입니다.** 직접 고치면 `/forge-update`·`install.ps1`이 되돌립니다. 수정은 반드시 claude-forge 저장소에서 하세요.
