# lesson-maker — 세션 지침

수학 교사가 Reveal.js 슬라이드·학습지를 만드는 저장소입니다.
**클라우드 세션에서 만들고, 맥·학교 윈도우 노트북에서 검토합니다. GitHub `main`이 유일한 보관소입니다.**

---

## 1. 작업 장소와 동기화 (반드시 지킬 것)

### 어디서 무엇을 하나

| 장소 | 역할 |
|---|---|
| **클라우드 세션** | 주 작업장 — 교과서 읽기, 차시 설계, 덱 제작, 기계적 검증 |
| **맥 · 윈도우 노트북** | **검토가 본업** — 실제로 열어 사람 눈으로 확인. 수업 전 필수 |
| **GitHub `main`** | 유일한 보관소. 어느 장소든 작업 결과는 여기로 올라간다 |

클라우드 세션의 파일은 임시다. **세션에서 나오기 전에 반드시 push한다.**
커밋하지 않은 작업은 사라진다고 간주할 것.

### 규칙 세 줄

1. **다른 쪽 세션이 열려 있으면 작업하지 않는다.** 번갈아 쓰는 건 안전하다 — *동시에* 쓰는 것만 문제다.
   원격에 모르는 커밋이 있으면 시작 전에 사용자에게 확인한다.
2. **로컬 세션은 열 때 `git pull --ff-only`.** 거부되면 로컬은 받기만 하는 자리이므로
   `git fetch origin && git reset --hard origin/main`으로 맞춰도 된다(사용자 확인 후).
3. **어디서 고쳤든 그 자리에서 바로 커밋·push.** 여러 시간 쥐고 있지 않는다 — 그 시간이 곧 충돌 창이다.

### 검토에서 나온 문제는 어디서 고치나

| 문제 | 고치는 곳 | 이유 |
|---|---|---|
| 내용 · 구조 · 새 차시 | 클라우드 | 한 번 고치면 끝이라 왕복 부담이 없다 |
| 넘침 · 겹침 · 잘림 | **로컬** | 재고 → 고치고 → 다시 재는 반복이 필요해 브라우저 앞이 빠르다 |

로컬에서 고치는 것은 예외가 아니라 정상 흐름이다. 규칙 1·3만 지키면 안전하다.

### 그 밖에

- **파일 삭제는 범위를 먼저 보고하고 확인받는다.** 몇 개를 왜 지우는지 목록으로 제시할 것.
- **규격 수치를 바꿨으면 커밋 메시지에 그 수치를 적는다.** (예: "세로 상한 994px 기준")
  적지 않으면 다른 장소가 다른 기준으로 되돌린다.

작업 이력·기기별 잔여 작업은 `.claude/handoff.md`, 설계 근거는 `docs/plans/YYYY-MM-DD-<주제>.md`에 남깁니다.
`~/.claude/plans/`는 기기를 넘지 못하므로 확정된 계획은 반드시 저장소에 복사합니다.

> **왜 이렇게 정했나 (2026-08-20)**
> 맥과 윈도우 세션이 12분 겹친 사이, 구판 삭제를 양쪽이 따로 하고 슬라이드 세로 상한을
> 서로 다른 값(1080 vs 994)으로 잡아 도식 크기가 왔다 갔다 했다. **git 충돌은 0건이었다** —
> 문제는 병합이 아니라 두 세션이 서로 모르는 채로 각자 판단한 것이었다.
> 그래서 편집 장소를 클라우드 한 곳으로 모으고, 규격을 이 문서에 못박았다.

---

## 2. 슬라이드 규격 (확정)

새 덱은 **가장 최근 차시 덱을 복사해서** 시작하는 것이 가장 안전합니다.
(현재 기준: `ai-math-2022/output/slides/슬라이드_1-3-2차시_빅데이터활용과공정성.html`)

### 크기

- Reveal 스테이지 **1920×1080**, `center: false`
- **한 장의 세로는 994px 이하.** 물리적 한계는 1080px이지만(측정 근거는 handoff 참조)
  기기·폰트 폴백 차이를 위해 994를 상한으로 쓴다.
- 검증은 **모든 버튼·표·카드를 펼친 상태**에서 `section.scrollHeight`로 잰다.

### 자산

```html
<script src="https://cdn.jsdelivr.net/npm/reveal.js@4.6.1/dist/reveal.js"></script>
<script src="https://cdn.jsdelivr.net/npm/reveal.js@4.6.1/plugin/notes/notes.js"></script>
...
plugins: [RevealNotes]
```

- CDN: reveal.js **4.6.1** / MathJax 3 / GSAP 3.12.5
- CSS: `../../../_shared/templates/{design-tokens,slide-system}.css` — 산출물은 3단계 깊이라
  `../../../`이다. 템플릿의 `../../`를 그대로 복사하면 404 난다.
- **`libs/` 경로를 쓰지 말 것.** 저장소에 존재한 적이 없다(구판 덱이 이걸 참조해 실행 불가였다).

### 코드 규칙

- **수식 안의 `<`는 항상 `&lt;`로 쓴다.** `\(x<h\)`처럼 `<` 뒤에 알파벳이 오면 브라우저가
  HTML 태그로 파싱해 그 줄을 통째로 삼킨다. 콘솔 에러도 MathJax 에러도 나지 않는다.
- `window.safeTypeset()`만 사용한다. `MathJax.typesetPromise()` 직접 호출 금지.
  동적으로 넣은 칸·step-box는 넣은 뒤 `window.safeTypeset([el])`을 호출한다.
- **색은 `--lm-*` 토큰만.** 하드코딩 금지 (SVG는 `<style>`의 클래스로).
- SVG는 `preserveAspectRatio="xMidYMid meet"`, `overflow:hidden`, 폰트 하한 0.7em
- step-box는 `.visible` 클래스만으로 표시한다.
- **새로 정의한 CSS 클래스는 실제로 마크업에서 쓰는지 확인한다.** 오늘 두 번,
  정의만 하고 쓰지 않아 강조가 화면에 나타나지 않는 버그가 있었다.

### 덱 골격

제목 → 지난 시간(앞 차시 마지막 문장 회수) → 오늘 도착할 곳 → 전개 → 확인 활동 → 오늘 한 줄(다음 차시 예고)

- **장마다 `<aside class="notes">`를 넣는다.** 교사가 `S` 키로 여는 발표자 노트다.
- 장수는 내용에 맞춘다. 형제 덱과 같아야 하는 건 골격이지 장수가 아니다.
- 파일명: `슬라이드_<절>-<차시>차시_<제목>.html`

### 내용 원칙

- **숫자는 교과서 값 그대로.** 학생 손의 책과 화면이 일치해야 한다.
  정답은 `<과목>/book/*정답및풀이.pdf`로 대조한다.
- 교과서에 없는 보조 예시를 넣을 때는 **화면에 그렇게 표시한다.**
- 교과서가 정한 설명 순서를 임의로 바꾸지 않는다.
  (2015 자료가 "퍼셉트론의 한계"부터 꺼내 꼬였던 전례가 있다 — `docs/plans/` 참조)

---

## 3. 검증

`.claude/launch.json`의 `lesson-maker-static`(포트 8791)으로 띄운다. 포트 8765는 피한다.

검증은 두 겹이고 **둘 다 필요하다.** 기계 검사를 다 통과해도 사람 눈 검토는 없어지지 않는다.

### 기계 검사 — 만든 자리에서 (가능하면 클라우드)

덱 하나를 끝냈다고 말하기 전에:

- [ ] 모든 인터랙션을 펼친 상태에서 세로 994px 넘김 0 · 가로 1920px 넘김 0
- [ ] 콘솔 에러 0 · 404 0 (favicon 제외)
- [ ] MathJax 오류 0 · 화면에 남은 원시 TeX 0 (`\(`, `\le` 등이 글자로 보이면 실패)
- [ ] 발표자 노트 개수 = 장수, `Reveal.getPlugins()`에 `notes` 포함
- [ ] 표·버튼 값이 교과서 정답과 일치 (`book/*정답및풀이.pdf` 대조)
- [ ] 핵심 장은 스크린샷으로 확인

브라우저를 쓸 수 없는 환경이면 **그 사실을 사용자에게 명시하고** 나머지 항목만 검사한다.
"검증했다"고 말하지 말 것.

### 사람 눈 검토 — 로컬에서, 수업 전 (항상)

기계가 못 잡는 것들이다. 실제로 2026-08-20에 이렇게 잡았다.

- 도식이 뒷자리에서 보일 만한 크기인가 (좌표평면·게이트가 작아 키운 사례)
- 라벨이 선이나 다른 라벨과 겹치지 않는가 (가중치 4와 2가 교차점에서 헷갈린 사례)
- 강조가 화면에 실제로 나타나는가 (CSS를 정의만 하고 안 써서 XOR 행 강조가 안 보인 사례)
- 설명 흐름이 수업 순서로 자연스러운가

---

## 4. 저장소 구조

```
ai-math-2022/     2022 개정 인공지능수학 — 현재 진행 중
  book/           교과서 PDF (지도서는 gitignore, Google Drive에서 복사)
  config/         visuals.md · animations.md (다크 테마 구판이라 개념만 참고)
  output/slides/  차시 단위 덱
ai-math/          2015 교육과정 — 참고용, 현재 진도와 무관
_shared/templates/  design-tokens.css · slide-system.css · reveal_base.html
docs/plans/       설계 근거 문서
.claude/handoff.md  기기 전환 인수인계 + 잔여 작업
```

제작 파이프라인 전반은 `orchestrator.md`, 사용법은 `README.md`를 본다.
이 파일은 그 둘과 겹치지 않는 **동기화 규칙과 확정된 슬라이드 규격**만 담는다.
